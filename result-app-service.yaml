apiVersion: v1
kind: Service
metadata: 
 name: result-app-service
 labels: 
  name: result-app-service
  app: voting-app
spec:
 type: LoadBalancer
 ports:
  - port: 80
    targetPort: 80
 selector:
  app: voting-app
  name: result-app-pod  
