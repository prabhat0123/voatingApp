apiVersion: v1
kind: Service
metadata: 
 name: voatingApp-Service
 labels: 
  name: voatingApp-Service
  app: voatingApp
spec: 
 type: LoadBalancer
 ports: 
  port: 80
  targetPort: 80
 selector: 
  name: voatingApp-pod
  app: voatingApp
